name: Sync to Org

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Push to Org
        run: |
          git remote add org https://x-access-token:${{ secrets.ORG_ACCESS_TOKEN }}@github.com/Phorex-Labz/phorex-labz-website.git
          git push org main --force